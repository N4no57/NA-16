1.5.5:
 - made symbol resolver make as many passes as required to resolve all symbol values

1.5.4:
 - fix the entire 2 pass symbol resolver
 - make 2 pass symbol resolver 3 pass
 - add ability to have '_' at the start and anywhere else in an identifier

1.5.3:
 - added special register usage of push and pop
 - movsr and movrs now can use SPR in register indirect

1.5.2:
 - added architecture for pseudo ops
 - MOV instruction can be used on all instructions and assembler corrects it

1.5.1:
 - fixed minor bug with symbols

1.5.0:
 - added symbols to assembler so that labels and constants can be used